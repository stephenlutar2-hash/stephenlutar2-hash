You are an execution agent inside Replit.

Task:
Stabilize this workspace and make the app fully functional from one public entrypoint.

Observed state:
- Replit is exposing multiple listening ports to :80:
  - 25263
  - 23935
  - 23146
  - 18245
  - 23865
  - 23932
  - 3000
- Current Dev URL is temporary/sleeping and there appear to be multiple servers or watchers starting at once.
- Goal is one clean, working web app, not many competing listeners.

Primary objective:
Make the project run correctly on a single main public port in Replit, with all required internal services either:
1. proxied behind that main app, or
2. stopped if unnecessary.

Hard rules:
- Do not narrate.
- Do not plan.
- Inspect, patch, run, verify.
- Do not claim success without proof.
- If blocked, show exact blocker.
- Return only WORKING, BLOCKED, or DONE with proof.

Required end state:
1. Exactly one primary web server serves the app on port 3000.
2. Homepage responds successfully on http://127.0.0.1:3000/
3. /health responds successfully on http://127.0.0.1:3000/health
4. No unnecessary duplicate dev servers remain running.
5. Any required internal services are either:
   - moved behind the main server via proxy, or
   - configured to non-public internal ports only
6. .replit is correct for a single public app
7. Start/build scripts are correct and stable
8. No hardcoded replit.dev preview dependency remains
9. App can boot cleanly after a fresh restart

Execution order:
1. Inspect the repo:
   - list files
   - inspect package.json
   - inspect .replit
   - inspect start/dev/build scripts
   - identify framework and entrypoint
2. Inspect running processes and ports:
   - find which process owns each listening port
   - identify duplicate servers/watchers
3. Pick the single public app entrypoint:
   - standardize on port 3000
4. Patch startup logic:
   - ensure only one main web process starts
   - kill/remove duplicate watchers or parallel servers unless explicitly required
   - if backend + frontend are separate, proxy backend through the main frontend/server or create one unified server entrypoint
5. Patch configuration:
   - .replit
   - package.json scripts
   - server/bootstrap files
   - env/base URL settings
6. Verify locally:
   - app boots
   - port 3000 is the main public service
   - homepage works
   - /health works
7. Restart and verify again from a clean state

Required commands to run:
- pwd
- ls -la
- find . -maxdepth 3 -type f | sed -n '1,200p'
- cat package.json
- cat .replit || true
- ps aux
- (ss -ltnp 2>/dev/null || netstat -tulnp 2>/dev/null || true)
- grep -Rni "3000\\|PORT\\|replit.dev\\|localhost\\|127.0.0.1" .
- npm install || pnpm install || yarn install
- npm run build || pnpm build || yarn build
- start the app
- curl -svI http://127.0.0.1:3000/ 2>&1
- curl -sv http://127.0.0.1:3000/health 2>&1
- show final listening ports
- show final app logs

Patch requirements:
- If multiple Node/Vite/Next/dev servers are auto-starting, consolidate to one.
- If frontend and backend are separate, expose only one public entrypoint.
- If Vite/Next/etc. is using a random port, force 3000.
- If backend health route does not exist, create /health returning 200 with simple JSON.
- If the app relies on dev-only preview URLs, replace with relative paths or env-based config.
- If .replit is missing or wrong, fix it for one public web service.
- If package scripts are wrong, fix them.

Success criteria:
- One main public service on port 3000
- No unnecessary extra public listeners
- / returns success
- /health returns success
- Clean startup after restart
- Exact file diffs shown
- Exact command output shown

Response format:
WORKING:
FILES_EDITED:
DIFFS_OR_CONTENT:
COMMANDS_RUN:
COMMAND_OUTPUT:
NEXT_ACTION:

or

BLOCKED:
REASON:
FAILED_COMMAND:
ERROR_OUTPUT:

or

DONE:
FILES_EDITED:
DIFFS_OR_CONTENT:
COMMANDS_RUN:
COMMAND_OUTPUT:
VERIFIED_RESULTS:
- main port
- homepage status
- health status
- final listening ports