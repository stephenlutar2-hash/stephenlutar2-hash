You are repairing an incomplete monorepo export and making every app runnable.

Context:
- The repo is a partial workspace export, not a complete project.
- Present app folders:
  - artifacts/aegis
  - artifacts/beacon
  - artifacts/lutar
  - artifacts/nimbus
  - artifacts/rosie
  - artifacts/api-server
  - artifacts/mockup-sandbox
- Existing package names already indicate the intended workspace:
  - @workspace/aegis
  - @workspace/beacon
  - @workspace/lutar
  - @workspace/nimbus
  - @workspace/rosie
  - @workspace/api-server
  - @workspace/mockup-sandbox
- The real blocker is missing monorepo backbone and missing shared workspace packages.

What is broken:
1. Missing root workspace files:
   - package.json
   - pnpm-workspace.yaml
   - tsconfig.base.json
   - .env.example
2. Missing shared packages:
   - lib/db
   - lib/api-zod
   - lib/api-client-react
3. Broken imports already exist in source:
   - api-server imports @workspace/db and @workspace/api-zod
   - multiple frontends import @workspace/api-client-react
4. Auth is insecure/incomplete:
   - artifacts/api-server/src/routes/auth.ts contains hardcoded user auth logic
   - multiple frontends use localStorage-only route guards
   - artifacts/lutar/src/pages/Landing.tsx contains a hardcoded default password value in UI state
5. The goal is not to redesign the apps. The goal is to restore architecture, preserve branding, and make them run.

Primary objective:
Recover and operationalize this monorepo so all apps build and the shared API works.

Execution requirements:
- Preserve the current app identities and UI themes unless a change is required for functionality or security.
- Reconstruct the workspace as a pnpm monorepo.
- If pnpm is not installed in the environment, install/configure it and continue.
- Do not remove features just to get builds green.
- Do not leave hardcoded secrets or demo passwords in source.
- Prefer minimal, surgical fixes.
- Use TypeScript throughout.
- Keep the API typed and shared across apps.

Build the following root files:
1. package.json
   - private true
   - packageManager pnpm@9
   - scripts:
     - dev:api
     - dev:aegis
     - dev:beacon
     - dev:lutar
     - dev:nimbus
     - dev:rosie
     - dev:mockup
     - build
     - typecheck
     - seed
     - seed:rosie
2. pnpm-workspace.yaml
   - packages:
     - artifacts/*
     - lib/*
3. tsconfig.base.json
   - shared TS settings compatible with Vite frontends and Node backend
4. .env.example
   - DATABASE_URL
   - PORT
   - BASE_PATH
   - CORS_ORIGIN
   - DEMO_ADMIN_USERNAME
   - DEMO_ADMIN_PASSWORD
   - SESSION_TTL_HOURS

Create these missing workspace packages:
1. lib/db
   - package name: @workspace/db
   - exports a Drizzle database client and schema
2. lib/api-zod
   - package name: @workspace/api-zod
   - exports shared Zod request/response schemas and TS types
3. lib/api-client-react
   - package name: @workspace/api-client-react
   - exports typed fetch client and React Query hooks used by frontends

Database requirements:
- Use drizzle-orm
- Prefer postgres
- If postgres setup blocks local recovery, use sqlite only as a documented local fallback
- Implement schema for:
  - sessions
  - beacon_metrics
  - beacon_projects
  - nimbus_predictions
  - nimbus_alerts
  - zeus_modules
  - zeus_logs
  - inca_projects
  - inca_experiments
  - dreamera_content
  - dreamera_campaigns
  - rosie_threats
  - rosie_incidents
  - rosie_scans
- Include id, createdAt, updatedAt where appropriate
- Normalize numeric fields properly

API requirements:
- Base route: /api
- Support:
  - GET /api/healthz
  - POST /api/auth/login
  - GET /api/auth/me
  - POST /api/auth/logout
  - CRUD or list/create/update/delete flows needed by current UI for:
    - beacon
    - nimbus
    - zeus
    - inca
    - dreamera
    - rosie
- Return consistent JSON error payloads
- Use zod validation
- Normalize numeric response fields before returning JSON

Auth repair requirements:
- Remove hardcoded credentials from backend auth route
- Remove any hardcoded password from frontend state defaults
- Replace with env-driven local demo auth or a seeded hashed admin user
- Centralize auth handling so frontends do not each invent their own logic
- Keep token-based auth if simplest, but make it secure enough for local demo/dev use
- Standardize one token key and one logout flow across apps

Frontend repair requirements:
- Fix all broken imports to shared workspace packages
- Keep Vite configs working
- Ensure each app can run independently against the shared API
- Preserve current pages and navigation
- Keep typed hooks in place by rebuilding the missing client package rather than ripping everything out

Known source locations to patch:
- artifacts/api-server/src/routes/auth.ts
- artifacts/api-server/src/routes/health.ts
- artifacts/api-server/src/routes/beacon.ts
- artifacts/api-server/src/routes/nimbus.ts
- artifacts/api-server/src/routes/zeus.ts
- artifacts/api-server/src/routes/inca.ts
- artifacts/api-server/src/routes/dreamera.ts
- artifacts/api-server/src/routes/rosie.ts
- artifacts/api-server/src/seed.ts
- artifacts/api-server/src/seed-rosie.ts
- artifacts/beacon/src/hooks/*
- artifacts/beacon/src/pages/*
- artifacts/nimbus/src/hooks/*
- artifacts/lutar/src/hooks/use-auth.ts
- artifacts/lutar/src/pages/Landing.tsx
- artifacts/*/src/App.tsx
- artifacts/*/src/pages/Login.tsx
- artifacts/rosie/src/pages/Dashboard.tsx

Validation steps:
1. Install dependencies
2. Typecheck all packages
3. Build:
   - @workspace/api-server
   - @workspace/aegis
   - @workspace/beacon
   - @workspace/lutar
   - @workspace/nimbus
   - @workspace/rosie
   - @workspace/mockup-sandbox
4. Start API and verify:
   - GET /api/healthz returns success
   - login flow works
   - protected routes work
   - CRUD flows used by UI no longer error
5. Seed demo data so dashboards are not empty

Do not do these:
- Do not replace the repo with a brand new starter app
- Do not remove auth entirely
- Do not hardcode new credentials into source
- Do not delete pages/modules to avoid fixing data flow
- Do not leave unresolved @workspace imports
- Do not stop at scaffolding; complete build and run validation

Deliverables:
1. Fully repaired repository
2. Exact list of created files
3. Exact list of modified files
4. Commands run
5. Any remaining blockers
6. Short remediation summary

Success definition:
- Root workspace exists
- Missing shared libs exist
- No unresolved workspace imports remain
- No hardcoded credentials remain
- API builds and starts
- All frontends build
- Demo auth works
- Seeded demo data loads into dashboards