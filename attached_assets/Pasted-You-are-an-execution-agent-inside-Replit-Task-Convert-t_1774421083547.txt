You are an execution agent inside Replit.

Task:
Convert this workspace into a stable single-port React/Vite + Express app for Replit.

Target architecture:
- Express is the only public server
- Express listens on port 3000
- React frontend is built with Vite
- Express serves the built frontend from dist in runtime
- API routes stay under /api
- Health route exists at /health
- No extra Vite/dev servers are exposed publicly in final runtime
- Replit maps only port 3000 to :80

Required end state:
1. One public process on port 3000
2. GET / returns the frontend successfully
3. GET /health returns HTTP 200 JSON
4. GET /api/* works through Express
5. Frontend uses relative API paths, not replit.dev or localhost hardcoding
6. Fresh restart boots cleanly
7. .replit and package scripts are correct

Rules:
- Do not narrate
- Do not plan
- Inspect, patch, run, verify
- Do not claim success without proof
- If blocked, show exact blocker
- Return only WORKING, BLOCKED, or DONE with proof

Execution steps:
1. Inspect workspace
- pwd
- ls -la
- find . -maxdepth 3 -type f | sed -n '1,250p'
- cat package.json
- cat .replit || true
- grep -Rni "vite\\|express\\|PORT\\|localhost\\|replit.dev\\|proxy\\|health" .

2. Identify current entrypoints
- determine frontend entry
- determine backend entry
- determine whether Express already exists
- determine whether Vite is only for dev or incorrectly running as public runtime

3. Standardize scripts in package.json
Required script intent:
- dev: run Express + Vite only for development if needed
- build: vite build
- start: node server entrypoint that serves dist and api on port 3000
If package manager is pnpm or yarn, preserve it consistently.

4. Create or patch a single Express server
Requirements:
- listen on process.env.PORT || 3000
- expose GET /health returning JSON like { "status": "ok" }
- serve /api routes before static frontend handler
- serve frontend build from dist
- fallback all non-/api non-/health routes to dist/index.html
- bind host 0.0.0.0

5. Patch Vite/frontend config
Requirements:
- build output goes to dist
- frontend API calls use relative /api paths
- remove hardcoded localhost and replit.dev URLs
- if env config exists, make production default to relative paths

6. Patch .replit
Goal:
- one main run command
- one public web service on port 3000
- no duplicate background dev servers in deployed runtime

7. Kill duplicate listeners and verify
Run:
- ps aux
- (ss -ltnp 2>/dev/null || netstat -tulnp 2>/dev/null || true)
- install deps
- run build
- start app
- verify only one main public server on 3000
- curl -svI http://127.0.0.1:3000/ 2>&1
- curl -sv http://127.0.0.1:3000/health 2>&1
- show final logs and final ports

Patch requirements:
- If no Express server exists, create one
- If server file is JS, keep JS unless TypeScript is already the backend standard
- If frontend and backend are split, unify runtime under Express serving dist
- If Vite dev server is auto-running in deployment, stop that and keep it dev-only
- If multiple random ports are listening, trace each process and remove unnecessary startup paths
- If /health is missing, add it
- If CORS/proxy hacks are only needed because of split ports, remove them where appropriate after unifying runtime

Expected files likely to change:
- package.json
- .replit
- server.js or server.ts or src/server.*
- vite.config.*
- frontend API config files
- env example files if needed

Success criteria:
- Exactly one main public app on port 3000
- / works
- /health works
- dist is served by Express
- no replit.dev dependency in app logic
- clean restart works

Response format:

WORKING:
FILES_EDITED:
DIFFS_OR_CONTENT:
COMMANDS_RUN:
COMMAND_OUTPUT:
NEXT_ACTION:

or

BLOCKED:
REASON:
FAILED_COMMAND:
ERROR_OUTPUT:

or

DONE:
FILES_EDITED:
DIFFS_OR_CONTENT:
COMMANDS_RUN:
COMMAND_OUTPUT:
VERIFIED_RESULTS:
- main port
- homepage status
- health status
- final listening ports
- startup command